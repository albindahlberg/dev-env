# Agent guide

This project's agent-facing context lives in `CLAUDE.md` and the `docs/` tree
it points to.

Read `CLAUDE.md` first, then `docs/INDEX.md` (the authority map). Everything
else follows from there.
