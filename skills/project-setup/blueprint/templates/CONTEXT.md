# Domain glossary

Core terms for this project's domain model. One definition per term. Each term
names the feature that owns it — the place where its logic and types live.

Keep this the single source for what a term means. Code comments and other
docs use the terms; they don't redefine them.

<!-- FILL: one entry per term, e.g.

## Term (native-language name, if the domain uses one)

What it is, in two or three sentences. Any identifying fields. How it relates
to the other terms. Owned by `<feature>`.

-->
